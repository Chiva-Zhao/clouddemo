server:
  port: 8255
spring:
  application:
    name: config-server-encrypt
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Chiva-Zhao/clouddemo_config
management:
  security:
    enabled: false
#          username: xxx
#          password: xxxity